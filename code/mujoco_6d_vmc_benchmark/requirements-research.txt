# Main primitive/workstation and matched-training source paths.
# Install a PyTorch build appropriate for your OS/CUDA separately if needed.
# Optional Pink/Autolife backends have additional dependencies; see SOURCE_GUIDE.md.
mujoco>=3.3,<4
numpy>=2,<3
scipy>=1.13,<2
gymnasium>=1,<2
Pillow>=10
imageio>=2.34
imageio-ffmpeg>=0.5
torch>=2.4,<3
